Object.assign(screens,{
landing(){const returning=state.status==='active'&&state.decision?`<div class="card"><div class="eyebrow">Незаконченное дело</div><h3>${esc(state.decision)}</h3><p class="small">${esc(domains[state.domain])} · ${esc(state.sessionId)}</p><div class="actions"><button class="btn black" id="resume">Продолжить</button></div></div>`:'';shell(`<div class="eyebrow">ONLINE COURSE / DEMENTOR CLUB</div><h1>ДУМАЙ<br><span class="acid-mark">С ОПАСНОСТЬЮ</span></h1><p class="lead">Курс последовательной деградации уверенности.</p><p class="copy"><b>Дементор: Валентин Лосев.</b></p><div class="card"><b>После курса вы сможете посмотреть на абсолютно нормальную ситуацию и обнаружить в ней минимум семь причин пока ничего не делать.</b></div><p class="copy">Вы работаете с одним настоящим решением. Финалом может быть: делать, изменить условия или не делать.</p>${returning}<div class="actions"><button class="btn" id="startBtn">Начать сомневаться</button><button class="btn alt" id="thinkBtn">Пока не поздно передумать</button></div><div class="small" id="already"></div>`)},
email(){shell(`<div class="eyebrow">Дело / вход</div><h2>Куда привязать это дело?</h2><p class="lead">На первой публичной стадии e-mail используется как локальный идентификатор вашей сессии.</p><label for="email">E-mail</label><input id="email" type="email" value="${esc(state.email)}" placeholder="you@example.com"><div class="note"><b>Stage 1:</b> адрес хранится только в этом браузере. Он не отправляется на сервер, и письмо сейчас не отправляется.</div>${err('emailErr')}<div class="footer-nav">${back('landing')}<button class="btn" id="emailNext">Продолжить</button></div>`)},
rule(){shell(`<div class="eyebrow">Дело / правило</div><h2>Нужно настоящее решение</h2><p class="lead">Такое, последствия которого вам действительно небезразличны.</p><div class="actions"><button class="btn" id="ruleYes">У меня есть такое</button><button class="btn alt" id="ruleNo">Нет. Я просто посмотреть</button></div>`)},
domain(){shell(`<div class="eyebrow">Дело / контекст</div><h2>Где происходит ваша проблема?</h2><div class="choice-grid">${Object.entries(domains).map(([k,v])=>`<button class="choice ${state.domain===k?'selected':''}" data-domain="${k}">${v}<small>${domainDescriptions[k]}</small></button>`).join('')}</div>${err('domainErr')}<div class="footer-nav">${back('rule')}<button class="btn" id="domainNext" ${state.domain?'':'disabled'}>Продолжить</button></div>`)},
decision(){shell(`<div class="eyebrow danger">Дело / решение</div><h2>Что вы собираетесь сделать?</h2><p class="lead">Выберите ближайшую формулировку действия или напишите свою. Выбрать можно только одну; после выбора текст можно поправить.</p>${editableSingle({key:'decision',presetKey:'decisionPreset',options:decisionOptions[state.domain]||[],value:state.decision,label:'Итоговая формулировка',placeholder:'Например: открыть второй офис до января'})}${err('decisionErr')}<div class="footer-nav">${back('domain')}<button class="btn" id="decisionNext">Продолжить</button></div>`)},
decisionConfirm(){shell(`<div class="eyebrow">Дело / проверка формулировки</div><h2>Итак</h2><div class="focus"><h3>${esc(state.decision)}</h3></div><p class="lead">Именно это решение будем портить следующие семь блоков.</p><div class="actions"><button class="btn" id="decisionConfirmBtn">Да. Это оно.</button><button class="btn alt" data-go="decision">Исправить</button></div>`)},
confidence(){shell(`<div class="eyebrow">Первичное показание</div><h2>Насколько это хорошая идея сейчас?</h2><p class="lead">Только один раз вы задаёте исходную уверенность сами. Дальше курс пересчитывает её автоматически по структуре ответов.</p><div class="result-number" id="cv">${state.initialConfidence}%</div><input id="confidence" type="range" min="0" max="100" value="${state.initialConfidence}"><div class="small">0 — идея уже умерла · 50 — посмотрим · 100 — невозможно представить проблемы</div><div class="footer-nav">${back('decisionConfirm')}<button class="btn" id="confidenceNext">Зафиксировать</button></div>`)},
valentinOpen(){valentinShell(`<div class="eyebrow">Валентин / первое вмешательство</div><h2>Решение у нас есть. Уверенность тоже.</h2><div class="quote">Теперь посмотрим, переживут ли они знакомство друг с другом.</div><div class="actions"><button class="btn" id="openCase">Открыть дело</button></div>`)},
caseCreated(){state.currentDay=1;state.status='active';caseId();save();shell(`<div class="eyebrow">Дело открыто</div><h2>${esc(state.sessionId)}</h2><div class="stats"><div class="stat"><span>Решение</span><b style="font-size:20px">${esc(state.decision)}</b></div><div class="stat"><span>Контекст</span><b style="font-size:20px">${esc(domains[state.domain])}</b></div><div class="stat"><span>Исходная уверенность</span><b>${state.initialConfidence}%</b></div></div><div class="actions">${btn('Перейти к первому подозрению','d1intro','black')}</div>`)},
d1intro(){dayIntro(1,'Да нормально будет','О — ожидание / П — предпосылки','d1expect')},
d1expect(){shell(`<div class="eyebrow danger">День 1 / О — ожидание</div><h2>Как выглядит успех?</h2><p class="lead">Выберите ближайший ожидаемый результат или свой вариант. После выбора отредактируйте формулировку.</p>${editableSingle({key:'expectedOutcome',presetKey:'expectedPreset',options:successOptions[state.domain]||[],value:state.expectedOutcome,label:'Ожидаемый результат'})}${err('expectedErr')}<div class="footer-nav">${back('d1intro')}<button class="btn" id="expectedNext">Продолжить</button></div>`)},
d1assume(){shell(`<div class="eyebrow danger">День 1 / П — предпосылки</div><h2>Что должно оказаться правдой?</h2><p class="lead">Выберите подходящие условия и добавьте свои. Выбранные пункты становятся редактируемыми. Нужно минимум семь.</p>${multiPicker({key:'assumptions',options:assumptionOptions[state.domain]||[],min:7,placeholder:'Добавьте условие, которого нет среди вариантов'})}${state.assumptions.length>=5?valentin(quotes.day1):''}${err('assumptionErr')}<div class="footer-nav">${back('d1expect')}<button class="btn" id="assumptionNext">Продолжить</button></div>`)},
d1classify(){const types=['ЗНАЮ','ПРЕДПОЛАГАЮ','МНЕ СКАЗАЛИ'];shell(`<div class="eyebrow">День 1 / классификация</div><h2>Что из этого вы действительно знаете?</h2><p class="lead">Для каждого условия выберите один статус.</p>${state.assumptions.map((a,i)=>`<div class="card"><b>${i+1}. ${esc(a)}</b><div class="actions">${types.map(t=>`<button class="btn ${state.assumptionTypes[i]===t?'':'alt'}" data-type-index="${i}" data-type="${t}">${t}</button>`).join('')}</div></div>`).join('')}${err('classErr')}<div class="footer-nav">${back('d1assume')}<button class="btn" id="classNext">Продолжить</button></div>`)},
d1control(){shell(`<div class="eyebrow danger">День 1 / контрольный вопрос</div><h2>На основании чего вы решили, что всё будет нормально?</h2><p class="lead">Выберите ближайшее основание или свой вариант. Ответ останется редактируемым.</p>${editableSingle({key:'control1',presetKey:'control1Preset',options:evidenceOptions.map(x=>x[1]),value:state.control1,label:'Ваше основание'})}${err('controlErr')}<div class="footer-nav">${back('d1classify')}<button class="btn" id="controlNext">Получить результат дня</button></div>`)},
d1summary(){summary(1,'d2intro')},
d2intro(){dayIntro(2,'Если тебе это сказали — это ещё не факт','обещание / способность / результат','d2agreement')},
d2agreement(){shell(`<div class="eyebrow danger">День 2 / договорённость</div><h2>На чьём обещании держится решение?</h2><p class="lead">Выберите ближайший вариант или свой; после выбора можно уточнить.</p>${editableSingle({key:'agreementWho',presetKey:'agreementWhoPreset',options:day2Who,value:state.agreementWho,label:'Кто именно?'})}<div class="option-title">Что именно человек обещал?</div>${editableSingle({key:'agreementSaid',presetKey:'agreementSaidPreset',options:promiseOptions,value:state.agreementSaid,label:'Формулировка обещания'})}${err('agreementErr')}<div class="footer-nav">${back('d2intro')}<button class="btn" id="agreementNext">Продолжить</button></div>`)},
d2dissect(){shell(`<div class="eyebrow">День 2 / разбор</div><h2>Разделите слова и выводы</h2><div class="option-title">Что вы из этого решили?</div>${editableSingle({key:'agreementInference',presetKey:'x1',options:inferOptions,value:state.agreementInference,label:'Ваш вывод'})}<div class="option-title">Что вы действительно знаете?</div>${editableSingle({key:'agreementKnown',presetKey:'x2',options:knownOptions,value:state.agreementKnown,label:'Факт'})}<div class="option-title">Как это можно проверить?</div>${editableSingle({key:'agreementCheck',presetKey:'x3',options:checkOptions,value:state.agreementCheck,label:'Способ проверки'})}${valentin(quotes.day2)}${err('dissectErr')}<div class="footer-nav">${back('d2agreement')}<button class="btn" id="dissectNext">Продолжить</button></div>`)},
d2cost(){const opts=['деньги','время','клиента','возможность','отношения','репутацию','почти ничего'];shell(`<div class="eyebrow">День 2 / цена доверия</div><h2>Что вы потеряете, если обещание не будет выполнено?</h2><div class="choice-grid">${opts.map(x=>`<button class="choice ${state.trustCost.includes(x)?'selected':''}" data-trust="${x}">${x}</button>`).join('')}</div>${err('costErr')}<div class="footer-nav">${back('d2dissect')}<button class="btn" id="costNext">Продолжить</button></div>`)},
});
