# Home hero asset

Upload the approved Home hero image here as:

- `home-1.webp`

This asset is the source image used by the main Home hero block on desktop and mobile. Do not create separate duplicate hero images unless a future approved design requires it.
