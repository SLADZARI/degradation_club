import {getClient,getEntryStatus,route} from '/community-runtime-v1.js';

const boardHost=document.getElementById('boardHost');
const filterHost=document.getElementById('boardFilters');
let viewport=null,entryStatus=null,activeIndex=0,cameraSnapshot=null;
function visibleCards(){return [...(boardHost?.querySelectorAll('.dc-notice[data-artifact]')||[])].filter(card=>!card.hidden&&!card.classList.contains('dc-board-filtered'))}
function stripBoardActions(){boardHost?.querySelectorAll('.dc-notice[data-artifact]').forEach(card=>{card.classList.remove('is-dragging');card.setAttribute('tabindex','0');card.setAttribute('role','button');card.setAttribute('aria-label',`${(card.querySelector('h3')?.textContent||'Объявление').trim()}. Открыть`);card.querySelectorAll('img').forEach(img=>img.draggable=false);if(!card.querySelector('.dc-board-open-hint')){const hint=document.createElement('div');hint.className='dc-board-open-hint';hint.textContent='ОТКРЫТЬ →';card.appendChild(hint)}})}
function cardHref(card){if(card.dataset.artifact)return route(`/community/artifact/${card.dataset.artifact}/`);return null}
function overlay(){let el=document.querySelector('.dc-artifact-overlay');if(el)return el;el=document.createElement('section');el.className='dc-artifact-overlay';el.hidden=true;el.setAttribute('aria-label','Открытое объявление');el.innerHTML='<div class="dc-artifact-overlay__scrim" data-overlay-close></div><div class="dc-artifact-overlay__panel"><button class="dc-artifact-overlay__close" type="button" data-overlay-close aria-label="Закрыть">×</button><iframe title="Объявление Dementor Club"></iframe></div>';document.body.appendChild(el);el.querySelectorAll('[data-overlay-close]').forEach(node=>node.addEventListener('click',closeOverlay));return el}
function openCard(card){const href=cardHref(card);if(!href)return;cameraSnapshot=boardHost?.style.transform||'';const el=overlay();el.querySelector('iframe').src=href;el.hidden=false;document.documentElement.dataset.boardArtifactOpen='1';el.querySelector('.dc-artifact-overlay__close')?.focus({preventScroll:true})}
function closeOverlay(){const el=document.querySelector('.dc-artifact-overlay');if(!el)return;el.hidden=true;el.querySelector('iframe').src='about:blank';if(boardHost&&cameraSnapshot!==null)boardHost.style.transform=cameraSnapshot;cameraSnapshot=null;delete document.documentElement.dataset.boardArtifactOpen}
function focusCard(card){if(!card||!viewport)return;const x=parseFloat(card.style.left)||0,y=parseFloat(card.style.top)||0,w=Math.max(card.offsetWidth,240),h=Math.max(card.offsetHeight,160);const rect=viewport.getBoundingClientRect(),scale=.92;boardHost.style.transform=`translate(${rect.width/2-(x+w/2)*scale}px,${rect.height/2-(y+h/2)*scale}px) scale(${scale})`;card.classList.remove('dc-board-focus-step');void card.offsetWidth;card.classList.add('dc-board-focus-step');setTimeout(()=>card.classList.remove('dc-board-focus-step'),900)}
function updateNavigator(){if(!filterHost)return;let nav=filterHost.querySelector('.dc-board-filter-nav');if(!nav){nav=document.createElement('div');nav.className='dc-board-filter-nav';nav.innerHTML='<button type="button" data-prev aria-label="Предыдущее объявление">←</button><span data-pos>0 / 0</span><button type="button" data-next aria-label="Следующее объявление">→</button>';filterHost.appendChild(nav);nav.querySelector('[data-prev]').onclick=()=>step(-1);nav.querySelector('[data-next]').onclick=()=>step(1)}const cards=visibleCards();if(activeIndex>=cards.length)activeIndex=0;nav.querySelector('[data-pos]').textContent=cards.length?`${activeIndex+1} / ${cards.length}`:'0 / 0';nav.hidden=cards.length<2}
function step(delta){const cards=visibleCards();if(!cards.length)return;activeIndex=(activeIndex+delta+cards.length)%cards.length;updateNavigator();focusCard(cards[activeIndex])}
function closeMembershipGate(){const gate=document.querySelector('.dc-board-membership-gate');if(!gate)return;gate.hidden=true;delete document.documentElement.dataset.boardMembershipGate}
function membershipGate(){let gate=document.querySelector('.dc-board-membership-gate');if(gate)return gate;gate=document.createElement('section');gate.className='dc-board-membership-gate';gate.hidden=true;gate.setAttribute('role','dialog');gate.setAttribute('aria-modal','true');gate.setAttribute('aria-label','Условия публикации на доске');gate.innerHTML='<button class="dc-board-membership-gate__scrim" type="button" data-membership-gate-close aria-label="Закрыть"></button><article class="dc-board-membership-gate__card"><button class="dc-board-membership-gate__close" type="button" data-membership-gate-close aria-label="Закрыть">×</button><div data-membership-gate-content></div></article>';document.body.appendChild(gate);gate.querySelectorAll('[data-membership-gate-close]').forEach(node=>node.addEventListener('click',closeMembershipGate));return gate}
function openMembershipGate(status){const gate=membershipGate();const host=gate.querySelector('[data-membership-gate-content]');const count=Math.max(0,Math.min(9,Number(status?.sphere_count||0)));const complete=status?.sphere_gate_complete===true||count>=9;const state=String(document.documentElement.dataset.dcBoardUserState||'');const applicant=state==='APPLICANT';let body='';if(applicant){body='<p>Базовый DC-9 уже пройден, а заявка на вступление находится в работе. Публикация откроется после подтверждения членства.</p><a class="dc-board-membership-gate__cta" href="'+route('/join/apply/')+'">СТАТУС ЗАЯВКИ →</a>'}else if(complete){body='<p>Базовый DC-9 уже пройден. Чтобы размещать публикации, осталось вступить в Dementor Club.</p><a class="dc-board-membership-gate__cta" href="'+route('/join/apply/')+'">ВСТУПИТЬ В КЛУБ →</a>'}else{body='<p>Чтобы приколоть объявление, нужно вступить в Dementor Club и пройти базовый квиз DC-9.</p><div class="dc-board-membership-gate__progress">DC-9 · '+count+' / 9</div><a class="dc-board-membership-gate__cta" href="'+route('/join/')+'">ПРОЙТИ DC-9 →</a>'}host.innerHTML='<div class="dc-board-membership-gate__meta">ПУБЛИКАЦИЯ / ДОСТУП</div><h2>ПРИКОЛОТЬ МОЖНО<br>ПОСЛЕ ВСТУПЛЕНИЯ.</h2>'+body;gate.hidden=false;document.documentElement.dataset.boardMembershipGate='1';gate.querySelector('.dc-board-membership-gate__close')?.focus({preventScroll:true})}
async function updatePrimary(){if(!viewport)return;let host=viewport.querySelector('.dc-board-primary');if(!host){host=document.createElement('div');host.className='dc-board-primary';host.innerHTML='<button type="button"></button>';viewport.appendChild(host)}const btn=host.querySelector('button');const client=getClient();let session=null;try{const auth=await client.auth.getSession();session=auth?.data?.session||null}catch{}try{entryStatus=await getEntryStatus(client)}catch{entryStatus=null}if(entryStatus?.membership_active!==true){if(!session?.user){host.classList.remove('is-visible');return}host.classList.add('is-visible');host.dataset.access='membership-required';btn.textContent='+ ПРИКОЛОТЬ';btn.onclick=()=>openMembershipGate(entryStatus||{});return}delete host.dataset.access;const available=Number(entryStatus.artifact_slots_available||0),consuming=Number(entryStatus.artifact_slots_consuming||0);host.classList.add('is-visible');if(available>0){btn.textContent='+ ПРИКОЛОТЬ';btn.onclick=()=>document.querySelector('.dc-spatial-controls [data-slot]')?.click()}else if(consuming>0){btn.textContent='МОЁ ОБЪЯВЛЕНИЕ';btn.onclick=()=>{const mine=boardHost?.querySelector('.dc-notice.is-own-movable')||boardHost?.querySelector('.dc-notice[data-artifact]');if(mine)openCard(mine)}}else host.classList.remove('is-visible')}
function moveFilters(){viewport=document.querySelector('.dc-spatial-viewport');if(!viewport||!filterHost)return false;if(filterHost.parentElement!==viewport)viewport.appendChild(filterHost);updateNavigator();return true}
function installInteractions(){document.addEventListener('dragstart',event=>{if(event.target.closest?.('.dc-notice[data-artifact]'))event.preventDefault()},true);document.addEventListener('click',event=>{const card=event.target.closest?.('.dc-notice[data-artifact]');if(!card||card.hidden||card.classList.contains('dc-board-filtered'))return;if(Number(card.dataset.boardJustDragged||0)>Date.now()-650)return;event.preventDefault();event.stopPropagation();openCard(card)},true);document.addEventListener('keydown',event=>{if(event.key==='Escape'&&document.documentElement.dataset.boardMembershipGate==='1'){closeMembershipGate();return}if(event.key==='Escape'&&document.documentElement.dataset.boardArtifactOpen==='1'){closeOverlay();return}const card=event.target.closest?.('.dc-notice[data-artifact]');if(card&&(event.key==='Enter'||event.key===' ')){event.preventDefault();openCard(card)}});window.addEventListener('dc:board-filter-changed',()=>{activeIndex=0;setTimeout(updateNavigator,0)});window.addEventListener('dc:board-projections-updated',()=>{stripBoardActions();updateNavigator()})}
async function init(){const wait=()=>new Promise(resolve=>{let tries=0;const tick=()=>{if(moveFilters()||tries++>50)return resolve();setTimeout(tick,60)};tick()});await wait();stripBoardActions();installInteractions();await updatePrimary();if(boardHost){let timer;new MutationObserver(()=>{clearTimeout(timer);timer=setTimeout(()=>{stripBoardActions();updateNavigator();updatePrimary()},90)}).observe(boardHost,{childList:true,subtree:true})}}
init().catch(error=>console.error('[DC Board fullscreen]',error));
