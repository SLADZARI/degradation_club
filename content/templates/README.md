# Content record templates

These files are implementation templates only. Copy a template when the corresponding entity is approved in its responsible source branch.

Rules:
- replace `null` only with approved facts;
- never publish a template itself as an entity;
- keep stable ID/slug once public;
- preserve `provenance` pointing to the canonical source;
- update section registers, sitemap and page URL after creation.
