name: Dementor Lab Human Result Once
on:
  push:
    paths:
      - '.github/workflows/dementor-lab-result-human-once.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/dementor-lab-vertical-slice-v0.3
      - name: Apply human result pass
        run: python3 dementor-lab/scripts/patch-result-human.py
      - name: Commit patch
        run: |
          rm dementor-lab/scripts/patch-result-human.py
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add dementor-lab .github/workflows/dementor-lab-result-human-once.yml
          git commit -m 'fix(lab): explain result like a human'
          git push origin HEAD:agent/dementor-lab-vertical-slice-v0.3
