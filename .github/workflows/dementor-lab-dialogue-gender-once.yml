name: Dementor Lab Dialogue Gender Once
on:
  push:
    paths:
      - '.github/workflows/dementor-lab-dialogue-gender-once.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/dementor-lab-vertical-slice-v0.3
      - name: Apply dialogue gender patch
        run: python3 dementor-lab/scripts/patch-dialogue-gender.py
      - name: Commit patch
        run: |
          rm dementor-lab/scripts/patch-dialogue-gender.py
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add dementor-lab .github/workflows/dementor-lab-dialogue-gender-once.yml
          git commit -m 'fix(lab): make dialogue gender aware'
          git push origin HEAD:agent/dementor-lab-vertical-slice-v0.3
