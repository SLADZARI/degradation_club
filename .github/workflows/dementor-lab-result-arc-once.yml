name: Dementor Lab Result Arc Once
on:
  push:
    paths:
      - '.github/workflows/dementor-lab-result-arc-once.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/dementor-lab-vertical-slice-v0.3
      - name: Project arc into result
        run: python3 dementor-lab/scripts/patch-result-arc-ui.py
      - name: Commit patch
        run: |
          rm dementor-lab/scripts/patch-result-arc-ui.py
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add dementor-lab/index.html dementor-lab/src/ui/app.mjs dementor-lab/tests/browser-smoke.mjs dementor-lab/scripts/patch-result-arc-ui.py
          git commit -m 'feat(lab): expose conversation arc in result'
          git push origin HEAD:agent/dementor-lab-vertical-slice-v0.3
