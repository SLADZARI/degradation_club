name: Dementor Lab Smoke First Run Wait Once
on:
  push:
    paths:
      - '.github/workflows/dementor-lab-smoke-first-run-wait-once.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/dementor-lab-vertical-slice-v0.3
      - name: Apply first-run smoke wait
        run: python3 dementor-lab/scripts/patch-smoke-first-run-wait.py
      - name: Commit patch
        run: |
          rm dementor-lab/scripts/patch-smoke-first-run-wait.py
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add dementor-lab .github/workflows/dementor-lab-smoke-first-run-wait-once.yml
          git commit -m 'test(lab): wait for first-run brain guide'
          git push origin HEAD:agent/dementor-lab-vertical-slice-v0.3
