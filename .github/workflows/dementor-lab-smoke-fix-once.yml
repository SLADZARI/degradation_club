name: Dementor Lab Smoke Fix Once
on:
  push:
    paths:
      - '.github/workflows/dementor-lab-smoke-fix-once.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/dementor-lab-vertical-slice-v0.3
      - name: Patch smoke
        run: python3 dementor-lab/scripts/patch-brain-smoke-remove-joke.py
      - name: Commit patch
        run: |
          rm dementor-lab/scripts/patch-brain-smoke-remove-joke.py
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add dementor-lab/tests/browser-smoke.mjs dementor-lab/scripts/patch-brain-smoke-remove-joke.py
          git commit -m 'test(lab): keep minimal graph valid before extra connect'
          git push origin HEAD:agent/dementor-lab-vertical-slice-v0.3
