name: Dementor Lab Talk Direction Once
on:
  push:
    paths:
      - '.github/workflows/dementor-lab-talk-direction-once.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/dementor-lab-vertical-slice-v0.3
      - name: Apply TALK direction patch
        run: python3 dementor-lab/scripts/patch-talk-stage-direction.py
      - name: Commit patch
        run: |
          rm dementor-lab/scripts/patch-talk-stage-direction.py
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add dementor-lab/src/ui/app.mjs dementor-lab/styles.css dementor-lab/tests/browser-smoke.mjs dementor-lab/scripts/patch-talk-stage-direction.py
          git commit -m 'feat(lab): direct talk arena around active turn'
          git push origin HEAD:agent/dementor-lab-vertical-slice-v0.3
