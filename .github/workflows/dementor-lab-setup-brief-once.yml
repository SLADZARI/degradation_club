name: Dementor Lab Setup Brief Once
on:
  push:
    paths:
      - '.github/workflows/dementor-lab-setup-brief-once.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/dementor-lab-vertical-slice-v0.3
      - name: Apply setup brief pass
        run: python3 dementor-lab/scripts/patch-setup-brief.py
      - name: Commit patch
        run: |
          rm dementor-lab/scripts/patch-setup-brief.py
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add dementor-lab .github/workflows/dementor-lab-setup-brief-once.yml
          git commit -m 'fix(lab): make setup a three-second brief'
          git push origin HEAD:agent/dementor-lab-vertical-slice-v0.3
