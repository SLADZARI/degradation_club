name: Dementor Lab Smoke Explicit Causality Once
on:
  push:
    paths:
      - '.github/workflows/dementor-lab-smoke-explicit-causality-once.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/dementor-lab-vertical-slice-v0.3
      - name: Apply explicit causality smoke patch
        run: python3 dementor-lab/scripts/patch-smoke-explicit-causality.py
      - name: Commit patch
        run: |
          rm dementor-lab/scripts/patch-smoke-explicit-causality.py
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add dementor-lab .github/workflows/dementor-lab-smoke-explicit-causality-once.yml
          git commit -m 'test(lab): author custom brain causality explicitly'
          git push origin HEAD:agent/dementor-lab-vertical-slice-v0.3
