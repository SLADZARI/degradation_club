name: Dementor Lab Scenario Switch Once
on:
  push:
    paths:
      - '.github/workflows/dementor-lab-scenario-switch-once.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/dementor-lab-vertical-slice-v0.3
      - name: Apply scenario switch UI
        run: python3 dementor-lab/scripts/patch-scenario-switch-ui.py
      - name: Commit patch
        run: |
          rm dementor-lab/scripts/patch-scenario-switch-ui.py
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add dementor-lab/src/ui/app.mjs dementor-lab/scripts/patch-scenario-switch-ui.py
          git commit -m 'feat(lab): expose second objective through same loop'
          git push origin HEAD:agent/dementor-lab-vertical-slice-v0.3
