name: Dementor Lab Readable Branches Once
on:
  push:
    paths:
      - '.github/workflows/dementor-lab-readable-branches-once.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/dementor-lab-vertical-slice-v0.3
      - name: Apply readable branch patch
        run: python3 dementor-lab/scripts/patch-readable-branches.py
      - name: Commit patch
        run: |
          rm dementor-lab/scripts/patch-readable-branches.py
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add dementor-lab/src/ui/app.mjs dementor-lab/tests/browser-smoke.mjs dementor-lab/scripts/patch-readable-branches.py
          git commit -m 'feat(lab): show readable side branches'
          git push origin HEAD:agent/dementor-lab-vertical-slice-v0.3
# rerun current patch
